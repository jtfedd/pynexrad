use crate::model::sweep_data::SweepData;

pub(crate) fn find_edges(
    labels: Vec<Vec<i32>>,
    num_masked_gates: i32,
    vel: &SweepData,
    max_gap_x: i32,
    max_gap_y: i32,
) -> ((Vec<i32>, Vec<i32>), Vec<i32>, (Vec<f32>, Vec<f32>)) {
    ()
}
